<x-layouts.admin title="User Aplikasi Inventory">
    <div class="admin-compact space-y-4">
        @include('admin.inventory.partials.header', ['icon' => 'users', 'title' => 'User Aplikasi', 'description' => 'Daftar akun aplikasi Flutter Inventory tanpa menampilkan credential.'])
        <form method="GET" class="flex gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><input name="search" value="{{ request('search') }}" placeholder="Cari nama, email, atau NIK" class="min-w-0 flex-1 rounded-lg border-slate-300"><button class="rounded-lg bg-blue-600 px-4 py-2 font-semibold text-white">Cari</button></form>
        <section class="overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50 text-left uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-3">Nama / Email</th><th class="px-4 py-3">Nomor Pegawai</th><th class="px-4 py-3">Departemen</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Login Terakhir</th><th class="px-4 py-3">Ganti Password</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($users as $inventoryUser)<tr><td class="px-4 py-3"><p class="font-semibold">{{ $inventoryUser->name }}</p><p class="text-slate-500">{{ $inventoryUser->email }}</p></td><td class="px-4 py-3">{{ $inventoryUser->employee_number ?? '-' }}</td><td class="px-4 py-3">{{ $inventoryUser->department ?? '-' }}</td><td class="px-4 py-3">{{ $inventoryUser->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="px-4 py-3">{{ $inventoryUser->last_login_at?->format('d/m/Y H:i') ?? 'Belum pernah' }}</td><td class="px-4 py-3">{{ $inventoryUser->must_change_password ? 'Wajib' : 'Tidak' }}</td></tr>@empty<tr><td colspan="6" class="px-4 py-8 text-center text-slate-500">Belum ada user aplikasi.</td></tr>@endforelse</tbody></table></div>@if (method_exists($users, 'links'))<div class="border-t border-slate-200 px-4 py-3">{{ $users->links() }}</div>@endif</section>
    </div>
</x-layouts.admin>
